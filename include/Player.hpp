#ifndef PLAYER_HPP
#define PLAYER_HPP

/////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

#include <TGUI/TGUI.hpp>

/////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

class Player : public sf::Transformable
{
public:

    Player(tgui::Gui& gui);

    void addToGui();

    void setMoving(bool moving);

    void update();

    sf::Vector2f getSize();

    void setSpeed(sf::Vector2f speed);
    sf::Vector2f getSpeed();

    /////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
private:

    tgui::Gui& m_gui;

    tgui::ext::SpriteSheet::Ptr m_image;

    sf::Vector2f m_speed;
    bool m_moving = false;
    bool m_animationInc = true;

    sf::Clock m_clock;
};

/////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

#endif // PLAYER_HPP
